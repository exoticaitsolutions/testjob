
#User Data Script
##Here’s the user data script to install Docker and add the `ec2-user` to the `docker` group:

```sh
#!/bin/bash
sudo yum update -y
sudo yum install -y docker
sudo service docker start
sudo usermod -a -G docker ec2-user
